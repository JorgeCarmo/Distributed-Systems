uddi.enabled=true

uddi.url=${uddi.url}
ws.name=${ws.name}
supplier.ws.name=${supplier.ws.name}
supplier.ws.url=${supplier.ws.url}
timeoutConnect=${timeoutConnect}
timeoutReceive=${timeoutReceive}

# ws url below is used only if uddi is disabled above
ws.url=${ws.url}
